name: Release - Hello World (Prod)

on:
  push:
    branches:
      - main

jobs:
  release-job:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello from Release
        run: echo "🔥 Hello World from RELEASE Pipeline (PROD)"

