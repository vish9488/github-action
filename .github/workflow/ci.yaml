name: CI - Hello World

on:
  workflow_dispatch:
  push:
    branches:
      - "feature/**"

jobs:
  ci-job:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello from CI
        run: echo "👋 Hello World from CI Pipeline"

